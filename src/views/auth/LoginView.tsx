const LoginView = () => {
  //   const { login } = useAuth();

  //   const handleLogin = async () => {
  //     await login();
  //   };

  return (
    <div>
      <button>Login</button>
    </div>
  );
};

export default LoginView;
